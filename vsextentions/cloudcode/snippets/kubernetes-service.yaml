name: kubernetes-service
label: Kubernetes Service
markdownDescription: Creates yaml for Kubernetes Service object
insertText: |2
  apiVersion: v1
  kind: Service
  metadata:
    name: ${1:appname}-service
  spec:
    type: ${2:ClusterIP}
    selector:
      app: ${1:appname}
    ports:
    - port: ${3:3000}
      targetPort: ${4:8080}
